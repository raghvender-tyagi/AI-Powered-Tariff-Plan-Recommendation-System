# Point this at your Express API, per the technical plan's client/.env contract.
# Leave unset during frontend-only development — the app falls back to demo data.
VITE_API_BASE_URL=http://localhost:5000/api
